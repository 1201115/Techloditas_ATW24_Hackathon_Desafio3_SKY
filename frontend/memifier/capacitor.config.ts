import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hackathon.memifier',
  appName: 'memifier',
  webDir: 'dist/memifier/browser'
};

export default config;
